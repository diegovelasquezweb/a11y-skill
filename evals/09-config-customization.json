{
  "id": "09-config-customization",
  "title": "Configuration management — dynamic updates",
  "skills": [
    "a11y"
  ],
  "query": "Configure the scanner to use a Mobile viewport (375x812) and limit it to 5 routes",
  "expected_behavior": [
    "Identifies existing `a11y.config.json` or proposes creating one",
    "Correctly sets `viewports` to `[{ \"width\": 375, \"height\": 812, \"name\": \"Mobile\" }]`",
    "Correctly sets `maxRoutes` to `5`",
    "Applies the changes to the JSON file using appropriate tools (e.g., write_to_file or replace_file_content)",
    "Confirms that changes are dynamic and will apply to the next audit run"
  ],
  "model_notes": {
    "all_agents": "Verifies the agent's ability to interpret and persist configuration instructions."
  }
}