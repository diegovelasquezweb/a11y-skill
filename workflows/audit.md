---
description: Run high-speed automated accessibility audit (Turbo Mode)
---

// turbo-all

1. Run the audit pipeline directly:

```
pnpm run audit -- --base-url http://localhost:3000
```

2. After completion, summarize the findings and mention the generated HTML report location.
